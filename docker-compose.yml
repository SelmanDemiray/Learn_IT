version: "3.8"
services:
  web:
    build: .
    ports:
      - "0:3000" # Map a random host port to container 3000
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
    environment:
      - NODE_ENV=production
